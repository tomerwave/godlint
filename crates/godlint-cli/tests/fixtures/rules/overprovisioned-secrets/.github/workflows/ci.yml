name: CI

on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: owner/action@v1
        with:
          all-secrets: ${{ secrets }}
        env:
          SERIALIZED_SECRETS: ${{ toJSON(secrets) }}


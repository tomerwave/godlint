version: 1

rules:
  security/forbidden-dependency:
    severity: error
    packages:
      - name: lodash
      - name: openssl
      - name: github.com/acme/legacy
      - name: "@corp/legacy"
      - name: requests
        allow-in:
          - src/vendor/**

name: CI

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: vendor/setup@v3
      - uses: vendor/setup@main
      - uses: vendor/setup@0f1a2b3c4d5e6f708192a3b4c5d6e7f8091a2b3c
      - uses: vendor/setup
      - uses: "vendor/quoted@v1"
      - uses: ./.github/actions/local
      - uses: docker://alpine:3.20
      - name: 'uses: vendor/commented@v1'
        run: 'echo "uses: vendor/string@v1"'

exit-code: 1

stdout: |
  src/app/service.rs:1:5: error[architecture/dependency-boundary] application must not depend on ui; the dependency runs against the declared layer order.
  src/domain/model.rs:1:5: error[architecture/dependency-boundary] domain must not depend on application; the dependency runs against the declared layer order.

stderr: ""

version: 1

rules:
  architecture/dependency-boundary:
    severity: error
    layers:
      - name: ui
        paths:
          - src/ui/**
        modules:
          - crate::ui
          - ui
          - "@app/ui"
      - name: application
        paths:
          - src/app/**
        modules:
          - crate::app
          - app
          - "@app/app"
      - name: domain
        paths:
          - src/domain/**
        modules:
          - crate::domain
          - domain
          - "@app/domain"

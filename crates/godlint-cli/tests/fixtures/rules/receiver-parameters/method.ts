class Service {
  accepted(one: number, two: number, three: number) {
    work(one, two, three);
  }

  reported(one: number, two: number, three: number, four: number) {
    work(one, two, three, four);
  }
}

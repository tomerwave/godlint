version: 1

exclude:
  - .github/workflows/excluded.yml

rules:
  ci/stale-action-refs:
    severity: error
    only-in:
      - "**/ci.yml"
      - "**/release.yml"
      - "**/allowed.yml"
    allow-in:
      - "**/allowed.yml"
